[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: every unknown path serves index.html for client-side routing.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
